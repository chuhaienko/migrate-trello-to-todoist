export interface FileDto {
	file_name: string;
	file_size: number;
	file_type: string;
	file_url: string;
	tn_l: [string, number, number];
	tn_m: [string, number, number];
	tn_s: [string, number, number];
}